import { observer } from "mobx-react-lite";
import React, { FC } from "react";


const Home: FC = () => {
  return (
    <div>
      домашняя страница
    </div>
  )
}

export default observer(Home);
